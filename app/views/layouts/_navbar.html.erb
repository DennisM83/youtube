<div class="bg-gray-200 py-4">
    <nav class="flex justify-between mx-5">
        <div>
            <span>YouTube Vids</span>
        </div>
        <div class="flex justify-evenly mx-11">
            <ul class="flex">
                <li class="mx-5">Home</li>
                <% if current_user %>
                    <li class="mx-5"><a href="#"><%= link_to  "Upload", new_post_path %></a></li>
                    <li class="mx-5"><%= button_to( "Log Out", destroy_user_session_path, method: :delete ) %></li>
                    <% else %>
                    <li><a href="#"><%=  link_to "Sign up", new_user_registration_path %></a></li>
                <% end %>
            </ul>
        </div>
    </nav>
</div>